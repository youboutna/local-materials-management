/**
 * @deprecated Ré-export de compatibilité.
 * Le checkpoint est une action de vérification d'un jalon : la définition
 * canonique vit désormais dans `./MilestoneDTO`.
 */

export type {
  CheckpointCategory,
  CheckpointDTO,
  CheckpointType,
  CheckpointVerificationResultDTO,
  VerificationItemDTO,
  VerificationStatus,
} from './MilestoneDTO';
