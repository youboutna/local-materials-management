/**
 * Project aggregate types — canonical definitions live in `@/dtos/entities/ProjectAggregateDTO`.
 * This module is kept as a compatibility re-export to avoid duplicate type declarations (rule R014).
 */
export * from '@/dtos/entities/ProjectAggregateDTO';
